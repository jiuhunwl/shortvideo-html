# ============================================================
# 依赖
# ============================================================
node_modules/

# ============================================================
# 环境变量
# .env.local / .env.*.local 放本地或私有配置，绝不能提交；
# .env.example 是模板，必须提交（下面的 ! 例外不能删）
# ============================================================
.env
.env.*
!.env.example

# ============================================================
# 构建缓存与临时文件
# ============================================================
.vite/
*.tsbuildinfo
.cache/
*.local

# ============================================================
# 编辑器 / 系统
# ============================================================
.idea/
.vscode/
.DS_Store
Thumbs.db
*~

# ============================================================
# 日志
# ============================================================
*.log
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# ============================================================
# 注意：dist/ 是本项目【有意提交】的构建产物（供 GitHub Pages 直接部署）。
# 已跟踪的 dist 文件不要忽略，也请勿把该目录加入上面的忽略列表；
# 重新构建后记得把新的 dist/assets/* 与被删除的旧哈希文件一并提交。
# ============================================================
